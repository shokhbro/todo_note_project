class Note {
  final int id;
  String title;
  String content;
  final DateTime createDate;

  Note({
    required this.id,
    required this.title,
    required this.content,
    required this.createDate,
  });
}
